export interface DecodedConfig {

    positioning: {
        width: string,
        margin :string,
        'margin-bottom': string; 
    };

    info: boolean;
    likeable: boolean;
    full: boolean;
    noMargin: boolean;
}